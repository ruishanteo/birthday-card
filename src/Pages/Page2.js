import { Box, Typography } from "@mui/material";

export function Page2() {
  return (
    <Box align="center" mt={5}>
      <Typography variant="h5">
        i think ure my compiler. my life won't start without u.
      </Typography>
      <Typography mt={8}>Text</Typography>
    </Box>
  );
}
