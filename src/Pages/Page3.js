import { Box, Typography } from "@mui/material";

export function Page3() {
  return (
    <Box align="center" mt={5}>
      <Typography variant="h5">
        are u a double? bc u alw float inside my head are you FIFO? bc ure a
        queueT
      </Typography>
      <Typography mt={8}>Text</Typography>
    </Box>
  );
}
