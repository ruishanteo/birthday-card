import { Box, Typography } from "@mui/material";

export function Page1() {
  return (
    <Box align="center" mt={5}>
      <Typography variant="h5">
        u're so beautiful u're like an exception, let me try and catch u
      </Typography>
      <Typography mt={8}>Text</Typography>
    </Box>
  );
}
